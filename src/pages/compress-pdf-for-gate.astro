---
import ExamPageLayout from "../components/ExamPageLayout.astro";
import { getEntry } from "astro:content";

const entry = await getEntry("examPages", "compress-pdf-for-gate");
if (!entry) throw new Error("Content entry not found: compress-pdf-for-gate");
const data = entry.data;
---

<ExamPageLayout {...data} />
