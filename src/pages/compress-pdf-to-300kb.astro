---
import ExamPageLayout from "../components/ExamPageLayout.astro";
import { getEntry } from "astro:content";

const entry = await getEntry("examPages", "compress-pdf-to-300kb");
if (!entry) throw new Error("Content entry not found: compress-pdf-to-300kb");
const data = entry.data;
---

<ExamPageLayout {...data} />
